import random

# 生成随机的循环控制条件和循环体内容
condition = random.choice([True, False])
loop_body = "Loop iteration"

# 生成带有循环控制语句的while循环
count = 0
while count < 10:
    print(loop_body)
    count += 1
    if condition:
        break
    else:
        continue
